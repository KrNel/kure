import React from 'react';
import Loading from '../Loading/Loading';

/**
 *  Component to show a loading spinner while user is being authenticated.
 *
 *  @returns {Component} - Displays loading spinner
 */
const Authorizing = () => (
  <Loading size="huge" />
);

export default Authorizing;
